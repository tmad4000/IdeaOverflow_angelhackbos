# Getting started
  
```	
  # if you don't have python installed, use brew install python or, apt-get install python for OSX and Linux, respectively.
  pip install Django==1.3
  pip install Divisi2
  pip install conceptnet==4
  
  
```

# 